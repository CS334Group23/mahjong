package sequence;

import java.util.HashMap;

import utils.Type;

public class ThirteenOrphans implements Sequence{
	private final HashMap<Integer, Type> sequence = {};
	
	@Override
	public Boolean isMatch(HashMap<Integer, Type> cards) {
		
	}
}
